gasback-lite-site/
├─ package.json              # à la racine du REPO (nécessaire pour les Actions)
├─ swap.html                 # page Jupiter
├─ scripts/                  # scripts exécutés par les Actions
│  ├─ build_volumes.js
│  ├─ snapshot_gasd.js
│  └─ build_payout.js
├─ data/                     # CSV/JSON produits par les Actions (commités)
│  ├─ volumes.csv            # généré chaque semaine
│  ├─ payout.csv             # généré chaque semaine
│  └─ snapshot.json          # généré au snapshot
├─ .github/
│  └─ workflows/
│     ├─ snapshot.yml        # snapshot GASD (lun 12:00 CET)
│     └─ weekly.yml          # volumes + payout (lun 12:05 CET)
└─ .gitignore                # ignore node_modules, secrets, etc.

Dossier local privé (hors Git) — ~/gasback-local/

gasback-local/
├─ package.json              # local uniquement (pour le script de paiement)
└─ pay_usdc_local.js         # script de paiement USDC (NE PAS committer)

